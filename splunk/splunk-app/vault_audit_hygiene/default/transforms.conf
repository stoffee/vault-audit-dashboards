# Baseline path inventory. Without this, a secret nobody has ever read is
# invisible: it emits no audit event, so no search over vault_audit can find it.
[vault_secret_inventory]
filename = vault_secret_inventory.csv
